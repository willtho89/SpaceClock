[project]
name = "spaceclock-esphome"
version = "0.1.0"
description = "ESPHome firmware for the SpaceClock / SpaceUhr project"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
  "esphome==2026.3.2",
]

[tool.uv]
package = false
